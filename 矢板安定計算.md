# 簡易土留Happy 設計仕様書 v01（雛形）

作成日: 2026-08-03
状態: **ドラフト**（未確定項目あり。第9章参照）
対象: 簡易土留（鋼矢板・軽量鋼矢板）の安定計算および矢板規格選定

---

## 0. 禁止事項・設計原則（最初に読むこと）

本章は Claude Code が実装に着手する前に必ず参照すること。
過去に仕様書の意図と実装側の事前想定が食い違った事例があるため、
先回りした「良かれと思った拡張」を禁止する。

### 0.1 禁止事項

- **根入れ部の受働抵抗を計算に含めないこと。** 根入れ長は本ツールの
  計算対象外である。安定計算から根入れ長を逆算する処理を追加しない。
- **弾塑性解析・連続ばり解法・たわみ角法を導入しないこと。** 適用範囲
  （第1章）を超える規模は計算対象外であり、警告を出して停止する。
- **ボイリング・ヒービング・盤ぶくれの照査を追加しないこと。** 別ツール
  の領域である。
- **SI単位系（kN, kPa）を内部計算に使用しないこと。** 第3章の単位系に
  従うこと。SI併記は出力オプションとしてのみ許容する。
- **矢板規格値をソースコードにハードコードしないこと。** 第7章の外部
  JSONから読み込むこと。
- **土圧強度を層ごとの解析式で場合分けして計算しないこと。** 第4章の
  数値積分方式に従うこと。

### 0.2 設計原則

- **数値積分による一様処理。** 1cm刻みのセルに分解し、各セルで土質・
  水位・上載荷重を判定する。層境・水位境で式を切り替える分岐を書かない。
  層数が増えても構造が変わらないことが本方式の目的である。
- **適用範囲外は計算しない。** 中途半端な結果を返すより停止する方が安全。
- **老朽機で動作すること。** Vostro 3500（1st-gen i7 / 8GB / Lubuntu）
  で実用速度で動くこと。Happyシリーズ共通の設計制約。

---

## 1. 適用範囲

| 項目 | 範囲 |
|---|---|
| 掘削深 H | 3.5 m 程度まで |
| 腹起し段数 | 2段（標準）、1段は構造的に成立しないため不可 |
| 土留め形式 | 鋼矢板・軽量鋼矢板 |
| 根入れ | 計算対象外（反力を期待しない） |

範囲を超える入力を受けた場合、計算を実行せず警告を表示して終了する。

---

## 2. 構造モデル

### 2.1 モデルの定義

矢板を鉛直方向の梁とみなし、以下の境界条件で解く。

- **上端: 自由端**（反力なし・モーメントなし）
- **下端: 自由端**（反力なし・モーメントなし）
- **支点: 腹起し2段のみ**

すなわち **両端に突出部を持つ単純ばり** である。
下端は横方向の支えが存在しないため、片持ち梁の突出部として扱う。

```
   ▽ 天端
   │ ← 上側突出部（片持ち）
   ●  R1  上段腹起し
   │
   │ ← スパン（単純ばり）
   │
   ●  R2  下段腹起し
   │ ← 下側突出部（片持ち）
   ▽ 掘削底面 = 矢板下端（自由端）
```

### 2.2 静定性

未知反力は R1, R2 の2つ。釣り合い式 ΣV=0、ΣM=0 の2式で解ける静定構造。
不静定解法は不要（第0.1章の禁止事項）。

### 2.3 設計思想

根入れ部の受働抵抗を全て無視するため、結果は安全側に出る。
簡易土留という用途において、この保守性は意図されたものである。

---

## 3. 単位系

内部計算・入力・表示は全て工学単位系（kgf/tf系）で統一する。

| 量 | 単位 |
|---|---|
| 側圧強度 p | tf/m² |
| せん断力 S | tf/m（幅1m当たり） |
| 曲げモーメント M | tf·m/m |
| 断面係数 Z | cm³/m |
| 許容曲げ応力度 σa | kgf/cm² |
| 単位体積重量 γ | tf/m³ |
| 長さ | m |

### 3.1 断面係数の算出時のみ単位換算

```
Z [cm³/m] = M [tf·m/m] × 100000 / σa [kgf/cm²]
```

例: M = 2.0 tf·m/m、σa = 1800 kgf/cm² のとき
Z = 2.0 × 100000 / 1800 ≒ 111 cm³/m

---

## 4. 計算方法

### 4.1 セル分割

天端から掘削底面まで、**1cm（0.01m）刻み**でセルに分割する。
セル数 N = H / 0.01

各セル i について、その深さ z_i における以下を判定する。

- 所属する土質層 → γ, Ka を決定
- 地下水位より上か下か → γ または γ' を使用
- 上載荷重の影響

### 4.2 側圧強度 p の算出

各セルで、背面側の主働土圧と水圧の和を側圧強度とする。

```
p_i = (鉛直有効応力 σv_i × Ka_i) + 水圧 u_i
```

- 鉛直有効応力 σv は上端から累加する
  - 地下水位より上: γ × Δ を加算
  - 地下水位より下: γ' × Δ を加算
  - 上載荷重 q は初期値として与える
- 水圧 u は地下水位からの深さ × 1.0 tf/m³
- 掘削側水位が背面水位より低い場合、その差が残留水圧として作用する

> **未確定**: Ka の与え方は第9章を参照。

### 4.3 せん断力 S

上端から累加する。

```
S_i = S_{i-1} + p_i × Δ        （Δ = 0.01 m）
```

腹起し位置のセルでは、反力 R を減じてSを不連続に落とす。

```
S_i = S_i - R        （腹起し設置深さのセル）
```

### 4.4 曲げモーメント M

せん断力をさらに累加する。

```
M_i = M_{i-1} + S_i × Δ
```

上端が自由端であるため M_0 = 0、S_0 = 0 から開始する。
下端でも M = 0、S = 0 に収束すること（検算に使用）。

### 4.5 反力の算出

釣り合い式から R1, R2 を求める。

- ΣV = 0: R1 + R2 = 全側圧の合計
- ΣM = 0: 任意点まわりのモーメント釣り合い

先に反力を確定させてから、4.3〜4.4 の累加を実行する。

### 4.6 最大曲げモーメント

M は配列として全深さ分を保持する。最大値は絶対値で判定する。

候補となる位置（参考。実装は配列の最大値探索で足りる）:

1. 上段腹起し位置（上側突出部による負曲げ）
2. 上段〜下段スパン内、S が符号反転する深さ（正曲げ）
3. 下段腹起し位置（下側突出部による負曲げ）

掘削深3.5m級では下側突出が長くなるため、3. が支配的になる場合が多い。

### 4.7 検算

以下を自動チェックし、逸脱時は警告を出す。

- 下端における S ≒ 0
- 下端における M ≒ 0

---

## 5. 入力仕様

`input.json` として与える。GUIからの入力を想定（tkinter）。

### 5.1 掘削条件

| 項目 | 記号 | 単位 | 備考 |
|---|---|---|---|
| 掘削深 | H | m | 適用範囲チェック対象 |
| 計算ピッチ | Δ | m | 0.01 固定 |
| 掘削幅 | B | m | 数量計算用。安定計算には影響しない |

### 5.2 支保工

| 項目 | 単位 | 備考 |
|---|---|---|
| 腹起し段数 | - | 2 |
| 上段設置深さ | m | 天端からの深さ |
| 下段設置深さ | m | 天端からの深さ |

### 5.3 土質層（上から順に複数）

| 項目 | 記号 | 単位 |
|---|---|---|
| 層厚 | - | m |
| 単位体積重量 | γ | tf/m³ |
| 水中単位体積重量 | γ' | tf/m³ |
| 主働土圧係数 | Ka | - |

層厚の合計は掘削深以上であること。

### 5.4 水位

| 項目 | 単位 | 備考 |
|---|---|---|
| 背面地下水位 | m | 天端からの深さ |
| 掘削側水位 | m | 釜場排水時は掘削底面と同値 |

### 5.5 上載荷重

| 項目 | 記号 | 単位 | 備考 |
|---|---|---|---|
| 過載荷重 | q | tf/m² | 10 kN/m² 相当 = 1.0 |

### 5.6 部材

| 項目 | 記号 | 単位 | 備考 |
|---|---|---|---|
| 許容曲げ応力度 | σa | kgf/cm² | SY295 仮設で 1800 が一般的 |
| 矢板規格表ファイル | - | - | 既定 `yaita_spec.json` |

---

## 6. 出力仕様

### 6.1 計算結果（JSON）

`dodome_data.json` として出力。後続ツール（作図・数量）が参照する。

- 反力 R1, R2
- 最大曲げモーメント M_max とその深さ
- 必要断面係数 Z_req
- 選定された矢板規格
- 側圧分布・S図・M図の配列データ

### 6.2 計算書

検査対応のため、計算過程を追える形式で出力する。
土圧分布図・S図・M図を含む。

### 6.3 DXF

断面図＋土圧分布図・S図・M図。
座標は mm（m × 1000）、`$INSUNITS = 4`。V-nas Clair 互換。

> Happyシリーズ共通の規約に従う。寸法線は寸法エンティティを使用し、
> 線+文字の自前描画に置き換えないこと（受領側からのクレーム実績あり）。

---

## 7. 矢板規格表（外部JSON）

`yaita_spec.json` として本体と分離する。
メーカーカタログの改訂や現場ごとの使用可能型の違いに対応するため、
コードにハードコードしない。

```json
[
  {"name": "LSP-2", "Z": 0.0, "t": 0.0, "w": 0.0},
  {"name": "LSP-3", "Z": 0.0, "t": 0.0, "w": 0.0}
]
```

| キー | 意味 | 単位 |
|---|---|---|
| name | 規格名 | - |
| Z | 断面係数（幅1m当たり） | cm³/m |
| t | 板厚 | mm |
| w | 単位重量 | kg/m² |

数値はジマさんが手元のカタログから記入すること。

### 7.1 選定ロジック

必要断面係数 Z_req を上回る最小の Z を持つ規格を選定する。
該当なしの場合は警告を出す。

---

## 8. 数量計算（後続モジュール）

安定計算とは分離する。本仕様書の対象外だが、前提のみ記載。

- 矢板長 = 掘削深 H + 余裕高さ 0.20 m
- 余裕高さは変更可能な定数として持つ

---

## 9. 未確定事項

### 9.1 主働土圧係数 Ka の与え方

以下のいずれかを選択する必要がある。**未決定。**

**案A: 層ごとに Ka を直接入力**
- 土質調査報告書・基準書の値をそのまま入力できる
- 検査時に数値の出所を説明しやすい
- 実装が最も軽い

**案B: 内部摩擦角 φ から計算**
- ランキン式 `Ka = tan²(45° − φ/2)`
- 入力は簡便
- 壁面摩擦角の扱いで流派が分かれ、検査時に説明を要する

現時点では **案A を推奨**。実務上、土質定数は与えられるものであり、
ツール側で解釈を加えない方が責任範囲が明確になるため。

---

## 10. 想定ツール構成

Happyシリーズの規約に従い、JSON経由でデータを受け渡す。

```
00_Dodome_Tougou.py      統括
tools/
  01_Dodome_Input.py     条件入力      → input.json
  02_Dodome_Doatsu.py    側圧分布計算  → doatsu_data.json
  03_Dodome_Danmenryoku.py 断面力計算  → dodome_data.json
  04_Dodome_Sentei.py    矢板規定選定
  05_Dodome_Zu.py        作図（DXF）
  06_Dodome_Keisansho.py 計算書出力
```

構成は暫定。実装着手前に確定させること。

---

## 11. 改訂履歴

| 版 | 日付 | 内容 |
|---|---|---|
| v01 | 2026-08-03 | 初版（雛形）。構造モデル・単位系・計算方法を確定。Ka の与え方は未決定 |